export enum CellType {
  Code = 'code',
  Markdown = 'markdown'
}

export enum EditorTheme {
  Dark = 'vs-dark',
  Light = 'vs-light',
  HighContrast = 'hc-black'
}

export enum MarkdownSyntaxType {
  Bold = '**',
  Italic = '*',
  InlineCode = '`',
  Heading = '# ',
  UnorderedList = '- ',
  OrderedList = '1. ',
  Quote = '> ',
  CodeBlock = '```\n\n```'
}
